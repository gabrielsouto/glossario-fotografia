const termos=[['Abertura','A abertura do diafragma controla a quantidade de luz que entra pela lente; é indicada pelo número f.','Exposição'],['Balanço de branco','Ajuste que busca neutralizar a cor da luz para que os tons pareçam naturais.','Cor'],['Bokeh','Qualidade visual das áreas desfocadas, especialmente nos pontos de luz ao fundo.','Lente'],['Compensação de exposição','Ajuste que clareia ou escurece a foto em relação à leitura automática da câmera.','Exposição'],['Distância focal','Característica da lente que influencia o ângulo de visão e a aproximação aparente da cena.','Lente'],['Exposição','Quantidade de luz registrada, determinada principalmente por abertura, velocidade e ISO.','Exposição'],['ISO','Sensibilidade usada pela câmera na captura; valores maiores ajudam em pouca luz, mas podem aumentar ruído.','Exposição'],['Profundidade de campo','Faixa da cena que parece nítida antes e depois do ponto de foco.','Foco'],['RAW','Formato que preserva mais dados da captura para edição posterior.','Arquivo'],['Velocidade do obturador','Tempo em que o obturador fica aberto; valores rápidos congelam melhor o movimento.','Exposição']];
function buscarTermos(consulta){const q=(consulta||'').trim().toLocaleLowerCase('pt-BR');return termos.filter(([t,d,c])=>!q||`${t} ${d} ${c}`.toLocaleLowerCase('pt-BR').includes(q));}
if(typeof document!=="undefined"){const input=document.querySelector('#busca'),lista=document.querySelector('#lista'),contador=document.querySelector('#contador');function render(){const r=buscarTermos(input.value);contador.textContent=`${r.length} termo${r.length===1?'':'s'} encontrado${r.length===1?'':'s'}`;lista.innerHTML=r.map(([t,d,c])=>`<article><span>${c}</span><h2>${t}</h2><p>${d}</p></article>`).join('')||'<p>Nenhum termo encontrado.</p>'}input.addEventListener('input',render);render()}
if(typeof module!=="undefined"&&module.exports)module.exports={buscarTermos,termos};
